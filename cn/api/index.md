# 核心 API

本章节介绍项目的核心 API。

## API 1

### 功能描述

这是一个核心 API 示例。

### 参数

- `param1`：参数 1 描述
- `param2`：参数 2 描述

### 返回值

返回一个对象，包含以下属性：
- `success`：操作是否成功
- `data`：返回的数据

### 示例

```javascript
const result = api1('value1', 'value2');
console.log(result);
```